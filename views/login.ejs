<!doctype html>
<html lang="cs"><head>
  <meta charset="utf-8"><meta name="viewport" content="width=device-width, initial-scale=1">
  <title>Přihlášení – Směny</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
  <link rel="stylesheet" href="/style.css">
</head><body>
  <%- include('partials/topbar') %>
  <div class="container container-narrow py-4">
    <div class="card p-4">
      <h1 class="h4 mb-3">Přihlášení</h1>
      <% if (error) { %><div class="alert alert-danger"><%= error %></div><% } %>
      <form method="post" action="/login" class="form-grid">
        <div>
          <label class="form-label">Osobní číslo</label>
          <input class="form-control" name="personal_number" required autofocus>
        </div>
        <div>
          <label class="form-label">Heslo</label>
          <input class="form-control" type="password" name="password" required>
        </div>
        <button class="btn btn-primary">Přihlásit</button>
      </form>
      <div class="mt-3">
        <a href="/forgot" class="link-secondary">Zapomenuté heslo</a> ·
        <a href="/privacy" class="link-secondary">Zásady zpracování údajů</a>
      </div>
    </div>
  </div>
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
</body></html>
