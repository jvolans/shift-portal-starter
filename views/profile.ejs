<!doctype html>
<html lang="cs"><head>
  <meta charset="utf-8"><title>Můj profil</title>
  <link rel="stylesheet" href="/style.css">
</head><body>
  <div class="topbar">
    <strong>Směny</strong>
    <nav>
      <a href="/me/shifts">Moje směny</a>
      <a href="/profile">Profil</a>
      <% if (user && user.is_admin) { %><a href="/admin">Admin</a><% } %>
    </nav>
    <div class="spacer"></div>
    <form method="post" action="/logout"><button class="btn-link">Odhlásit</button></form>
  </div>

  <div class="container">
    <h1>Můj profil</h1>
    <% if (message) { %><div class="notice"><%= message %></div><% } %>
    <% if (error)   { %><div class="alert"><%= error %></div><% } %>

    <p><strong>Osobní číslo:</strong> <%= user.personal_number %></p>
    <p><strong>Jméno:</strong> <%= user.full_name %></p>

    <h2>Kontakt</h2>
    <form method="post" class="card">
      <label>Telefon</label><input name="phone" value="<%= user.phone || '' %>">
      <label>E-mail</label><input name="email" value="<%= user.email || '' %>">
      <button type="submit">Uložit</button>
    </form>

    <h2>Změna hesla</h2>
    <form method="post" action="/profile/change-password" class="card">
      <label>Aktuální heslo</label><input type="password" name="current_password" required>
      <label>Nové heslo</label><input type="password" name="new_password" required>
      <button type="submit">Změnit heslo</button>
    </form>

    <p><a href="/me/shifts">← Zpět na moje směny</a></p>
  </div>
</body></html>
